const express = require('express');
const router = express.Router();

// Minimal AI routes placeholder
router.get('/', (req, res) => {
  res.json({ success: true, message: 'AI endpoints placeholder' });
});

module.exports = router;
